import {StyleSheet} from 'react-native';

export const makeRuletaScreenStyles = () =>
  StyleSheet.create({
    container: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
    },
  });
