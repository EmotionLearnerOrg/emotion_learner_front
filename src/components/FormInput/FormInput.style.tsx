import {StyleSheet} from 'react-native';

export const makeFormInputStyle = () =>
  StyleSheet.create({
    input: {
      width: '100%',
      height: 40,
      backgroundColor: '#ECF0F1',
      borderRadius: 5,
      paddingHorizontal: 10,
      color: '#34495E',
    },
  });
