import {StyleSheet} from 'react-native';

export const makeHeaderContainerStyles = () =>
  StyleSheet.create({
    headerContainer: {
      flexDirection: 'row',
      marginLeft: 'auto',
    },
  });
