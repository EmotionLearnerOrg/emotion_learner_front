import { StyleSheet } from 'react-native';

export const makeCardStyles = () => StyleSheet.create({
    cardContainer: {
        borderRadius: 6,
        alignItems: 'center',
        justifyContent: 'center',
        padding: 10,
    },
});
